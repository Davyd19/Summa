package com.app.summa.ui.theme

import androidx.compose.ui.graphics.Color

// Brutalist palette
// Brutalist palette
val BrutalBlue = Color(0xFF0044FF) // Vibrant Electric Blue
val BrutalBlack = Color(0xFF050505) // Deep Black
val BrutalWhite = Color(0xFFFAFAFA) // Stark White
val BrutalPaper = Color(0xFFF0F0F0) // Light Grey Paper
val BrutalGray = Color(0xFF202020)
val BrutalMuted = Color(0xFFE0E0E0)

// === PRIMARY COLORS - Modern & Sophisticated ===
val DeepTeal = Color(0xFF0D9488) // Teal 600 - Main brand
val TealLight = Color(0xFF5EEAD4) // Teal 300
val TealDark = Color(0xFF0F766E) // Teal 700
val TealContainer = Color(0xFFCCFBF1) // Teal 100

// === ACCENT COLORS - Vibrant & Energetic ===
val GoldAccent = Color(0xFFFBBF24) // Amber 400 - Brighter gold
val GoldLight = Color(0xFFFDE68A) // Amber 200
val GoldContainer = Color(0xFFFEF3C7) // Amber 100
val GoldDark = Color(0xFFD97706) // Amber 600

// === NEUTRAL COLORS - Soft & Modern ===
val OffWhite = Color(0xFFFAFAFA) // Gray 50
val WarmGray = Color(0xFF9CA3AF) // Gray 400
val LightGrayBorder = Color(0xFFE5E7EB) // Gray 200
val DeepCharcoal = Color(0xFF1F2937) // Gray 800
val SoftWhite = Color(0xFFF8FAFC) // Slate 50

// === DARK MODE COLORS - Rich & Comfortable ===
val DarkBackground = Color(0xFF0A0E14) // Very dark blue-gray
val DarkSurface = Color(0xFF151B23) // Slightly lighter
val DarkSurfaceVariant = Color(0xFF1E293B) // Slate 800
val DarkElevated = Color(0xFF273444) // For cards

// === SEMANTIC COLORS - Clear & Accessible ===
val SuccessGreen = Color(0xFF10B981) // Emerald 500
val SuccessGreenLight = Color(0xFF34D399) // Emerald 400
val SuccessGreenDark = Color(0xFF059669) // Emerald 600
val SuccessGreenBg = Color(0xFFD1FAE5) // Emerald 100

val WarningOrange = Color(0xFFF97316) // Orange 500
val WarningOrangeLight = Color(0xFFFB923C) // Orange 400
val WarningOrangeBg = Color(0xFFFFEDD5) // Orange 100

val ErrorRed = Color(0xFFEF4444) // Red 500
val ErrorRedLight = Color(0xFFF87171) // Red 400
val ErrorRedDark = Color(0xFFDC2626) // Red 600
val ErrorRedBg = Color(0xFFFEE2E2) // Red 100

val InfoBlue = Color(0xFF3B82F6) // Blue 500
val InfoBlueLight = Color(0xFF60A5FA) // Blue 400
val InfoBlueBg = Color(0xFFDCEFF9) // Blue 100

// === FEATURE COLORS - Distinctive & Memorable ===
val PurpleAccent = Color(0xFF8B5CF6) // Violet 500
val PurpleLight = Color(0xFFA78BFA) // Violet 400
val PurpleBg = Color(0xFFEDE9FE) // Violet 100

val BlueAccent = Color(0xFF3B82F6) // Blue 500
val BlueLight = Color(0xFF60A5FA) // Blue 400
val BlueBg = Color(0xFFDCEFF9) // Blue 100

val PinkAccent = Color(0xFFEC4899) // Pink 500
val PinkLight = Color(0xFFF472B6) // Pink 400
val PinkBg = Color(0xFFFCE7F3) // Pink 100

val IndigoAccent = Color(0xFF6366F1) // Indigo 500
val IndigoLight = Color(0xFF818CF8) // Indigo 400
val IndigoBg = Color(0xFFE0E7FF) // Indigo 100

// === HABIT STREAK COLORS ===
val StreakOrange = Color(0xFFF97316) // Fire emoji
val StreakGold = Color(0xFFFBBF24) // Crown emoji
val StreakBronze = Color(0xFFCD7F32) // Bronze medal
val StreakSilver = Color(0xFFC0C0C0) // Silver medal

// === MONEY COLORS ===
val MoneyGreen = Color(0xFF10B981) // Income
val MoneyRed = Color(0xFFEF4444) // Expense
val MoneyBlue = Color(0xFF3B82F6) // Transfer
val MoneyGold = Color(0xFFFBBF24) // Investment

// === GRADIENT COLORS ===
val GradientTealStart = Color(0xFF14B8A6)
val GradientTealEnd = Color(0xFF0D9488)

val GradientGoldStart = Color(0xFFFDE047)
val GradientGoldEnd = Color(0xFFFBBF24)

val GradientPurpleStart = Color(0xFFA78BFA)
val GradientPurpleEnd = Color(0xFF8B5CF6)

val GradientBlueStart = Color(0xFF60A5FA)
val GradientBlueEnd = Color(0xFF3B82F6)

val GradientPinkStart = Color(0xFFF472B6)
val GradientPinkEnd = Color(0xFFEC4899)

// === SURFACE TINTS ===
val SurfaceTintPrimary = Color(0xFF0D9488).copy(alpha = 0.05f)
val SurfaceTintSecondary = Color(0xFFFBBF24).copy(alpha = 0.05f)
val SurfaceTintSuccess = Color(0xFF10B981).copy(alpha = 0.05f)
val SurfaceTintError = Color(0xFFEF4444).copy(alpha = 0.05f)

// === PLANNER SPECIFIC (BARU) ===
// Komitmen: Gelap, Solid, Berat (Jangkar)
val CommitmentColor = Color(0xFF2C3E50)
val CommitmentContainer = Color(0xFF34495E)

// Aspirasi: Terang, Ringan, Putus-putus (Awan)
val AspirationColor = Color(0xFF95A5A6)
val AspirationContainer = Color.Transparent // Biar terlihat ringan